<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.StockKING" parent="Theme.AppCompat.Light">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/sub_color</item>
        <item name="colorPrimaryDark">@color/main_black_color</item>
        <item name="colorPrimaryVariant">@color/sub_color</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/white</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
    </style>

    <style name="TitleStyle" parent="android:TextAppearance">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">20dp</item>
        <item name="android:textColor">@color/main_black_color</item>
        <item name="android:fontFamily">@font/notosans_regular</item>
        <item name="android:includeFontPadding">false</item>
    </style>
    <style name="TextStyle" parent="android:TextAppearance">
        <item name="android:textSize">130dp</item>
        <item name="android:textColor">@color/main_black_color</item>
        <item name="android:fontFamily">@font/notosans_regular</item>
        <item name="android:includeFontPadding">false</item>
    </style>

    <style name="switchTextAppearance">
        <item name="android:textSize">13dp</item>
        <item name="android:textColor">@color/main_black_color</item>
        <item name="android:fontFamily">@font/notosans_regular</item>
    </style>

</resources>